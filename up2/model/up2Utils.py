#!/usr/bin/python
import random
import math

class utils:

    def randReg(self,bit,length):
        reg = []
        for i in range(0,length):
            reg.append(random.randint(0,math.pow()))
        return reg



